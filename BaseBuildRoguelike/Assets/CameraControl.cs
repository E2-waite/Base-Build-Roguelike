using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraControl : MonoBehaviour
{
    //    Vector2 lastPos;
    //    public float moveSpeed = 15;
    //    public float dist = 10;
    //    // Start is called before the first frame update
    //    void Start()
    //    {
    //        Cursor.lockState = CursorLockMode.Confined;
    //        lastPos = Input.mousePosition;
    //        transform.position = new Vector3(transform.position.x, transform.position.y, -dist);
    //    }

    //    // Update is called once per frame
    //    void Update()
    //    {
    //        if (Input.GetMouseButtonDown(1))
    //        {
    //            Cursor.lockState = CursorLockMode.Locked;
    //            Cursor.visible = false;
    //        }
    //        if (Input.GetMouseButtonUp(1))
    //        {
    //            Cursor.lockState = CursorLockMode.None;
    //            Cursor.visible = true;
    //        }

    //        if (Input.GetMouseButton(1))
    //        {
    //            if (Input.GetAxis("Mouse X") != 0 || Input.GetAxis("Mouse Y") != 0)
    //            {
    //                Vector3 newPos = new Vector3(transform.position.x - (Input.GetAxis("Mouse X") * (moveSpeed * Time.deltaTime)), transform.position.y - (Input.GetAxis("Mouse Y") * (moveSpeed * Time.deltaTime)), -dist);
    //                transform.position = newPos;
    //            }
    //        }
    //    }
}
